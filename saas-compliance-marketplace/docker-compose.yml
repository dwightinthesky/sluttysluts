services:
  saas-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: saas-compliance-backend
    ports:
      - "8090:8090"
    environment:
      PORT: "8090"
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
